import React from 'react'
import Line from '../Line'

const TitleCard = ({title}) => {
    return (
        <div className="text-lg text-gray-50 mb-2 font-bold px-8">
            <span>{title}</span>
         
        </div>
    )
}

export default TitleCard
